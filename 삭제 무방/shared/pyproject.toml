[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "regression-platform-shared"
version = "0.1.0"
description = "공유 유틸리티 및 상수"
requires-python = ">=3.11"
dependencies = [
    "pydantic>=2.7.0",
]

[tool.hatch.build.targets.wheel]
packages = ["shared"]
